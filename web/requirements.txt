# Scripture Engine — Python Dependencies
# Install with: pip install -r web/requirements.txt
# For development: pip install -r web/requirements.txt pytest mypy ruff

fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0
httpx>=0.28.0
biblical-transliteration>=0.3.0
python-dotenv>=1.0.0

# Optional: transformer embeddings for semantic search
# fastembed>=0.8.0
# sentence-transformers>=3.0.0

# Optional: audio alignment
# faster-whisper>=1.0.0
