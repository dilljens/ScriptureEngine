[pytest]
testpaths = tests
markers =
    slow: production-DB scans, minute-long graph traversals, or tests needing
        optional native deps (sqlite_vec). Skipped by default via addopts;
        run them explicitly with `pytest -m slow` or `pytest -m ""`.
addopts = -m "not slow"
